using System;
using System.Collections.Generic;

namespace mtgparser
{
    public enum Color
    {
        White,
        Blue,
        Black,
        Red,
        Green
    }

    public enum Set {
        ANA
    }
}